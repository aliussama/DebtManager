<UserControl x:Class="DebtManager.Desktop.Controls.EmptyStateControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400"
             x:Name="Root">

    <UserControl.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource App.AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,20,0,0"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <StackPanel VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                MaxWidth="400">
        <!-- Icon -->
        <Border Background="{DynamicResource App.SurfaceAltBrush}"
                CornerRadius="16" Width="64" Height="64"
                HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="{Binding IconText, ElementName=Root}" 
                       FontFamily="Segoe MDL2 Assets"
                       FontSize="24" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource App.TextSecondaryBrush}"/>
        </Border>

        <!-- Title -->
        <TextBlock Text="{Binding Title, ElementName=Root}" 
                   FontSize="18" 
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource App.TextPrimaryBrush}"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   TextAlignment="Center"/>

        <!-- Message -->
        <TextBlock Text="{Binding Message, ElementName=Root}" 
                   FontSize="13"
                   Foreground="{DynamicResource App.TextSecondaryBrush}"
                   HorizontalAlignment="Center"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   Margin="0,8,0,0"
                   LineHeight="20"/>

        <!-- Action Button -->
        <Button Content="{Binding ActionText, ElementName=Root}"
                Command="{Binding ActionCommand, ElementName=Root}"
                Style="{StaticResource ActionButton}"
                HorizontalAlignment="Center"
                Visibility="{Binding IsActionVisible, ElementName=Root, Converter={StaticResource BoolToVis}}"/>
    </StackPanel>
</UserControl>

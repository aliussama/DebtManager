<UserControl x:Class="DebtManager.Desktop.Controls.TagEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="60" d:DesignWidth="400"
             x:Name="Root">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <StackPanel>
        <!-- Tag chips -->
        <ItemsControl ItemsSource="{Binding Tags, ElementName=Root}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{DynamicResource App.SurfaceAltBrush}"
                            CornerRadius="10"
                            Padding="8,4"
                            Margin="0,0,6,4">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding}"
                                       Foreground="{DynamicResource App.TextPrimaryBrush}"
                                       FontSize="12"
                                       VerticalAlignment="Center"/>
                            <Button Content="&#xD7;"
                                    FontSize="12"
                                    FontWeight="Bold"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="{DynamicResource App.TextSecondaryBrush}"
                                    Cursor="Hand"
                                    Padding="4,0,0,0"
                                    VerticalAlignment="Center"
                                    Command="{Binding RemoveTagCommand, ElementName=Root}"
                                    CommandParameter="{Binding}"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Input row -->
        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
            <TextBox Text="{Binding NewTagText, ElementName=Root, UpdateSourceTrigger=PropertyChanged}"
                     Width="160"
                     FontSize="12"
                     Padding="6,4"
                     VerticalContentAlignment="Center"
                     Background="{DynamicResource App.SurfaceBrush}"
                     Foreground="{DynamicResource App.TextPrimaryBrush}"
                     BorderBrush="{DynamicResource App.BorderBrush}"/>
            <Button Content="Add"
                    Command="{Binding AddTagCommand, ElementName=Root}"
                    FontSize="12"
                    Padding="10,4"
                    Margin="6,0,0,0"
                    Background="{DynamicResource App.AccentBrush}"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>
    </StackPanel>
</UserControl>

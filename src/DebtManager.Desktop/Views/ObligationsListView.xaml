<UserControl x:Class="DebtManager.Desktop.Views.ObligationsListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:DebtManager.Desktop.Controls"
             xmlns:behaviors="clr-namespace:DebtManager.Desktop.Behaviors"
             xmlns:services="clr-namespace:DebtManager.Desktop.Services"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="{DynamicResource App.BackgroundBrush}">

    <UserControl.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource App.AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="2,0"/>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Foreground" Value="{DynamicResource App.DangerBrush}"/>
        </Style>

        <Style x:Key="SuccessButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Foreground" Value="{DynamicResource App.SuccessBrush}"/>
        </Style>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Obligations" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="{DynamicResource App.TextPrimaryBrush}"/>
                <TextBlock Text="{Binding StatusText}" 
                           FontSize="13"
                           Foreground="{DynamicResource App.TextSecondaryBrush}"
                           Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Export CSV" 
                        Command="{Binding ExportCsvCommand}"
                        Padding="12,8"
                        Margin="0,0,8,0"/>
                <Button Content="Refresh" 
                        Command="{Binding RefreshCommand}"
                        Padding="12,8"
                        Margin="0,0,8,0"/>
                <Button Content="+ Create Obligation" 
                        Command="{Binding CreateObligationCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Padding="16,8"/>
            </StackPanel>
        </Grid>

        <!-- Filter/Search Bar -->
        <Border Grid.Row="1" Background="{DynamicResource App.SurfaceBrush}" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <TextBox Grid.Column="0" 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         FontSize="14"
                         Padding="10,8"
                         MaxWidth="300"
                         HorizontalAlignment="Left"
                         behaviors:FocusOnRequestBehavior.FocusKey="{x:Static services:FocusTargets.ObligationsSearch}"
                         behaviors:FocusOnRequestBehavior.FocusService="{DynamicResource FocusService}">
                    <TextBox.Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Setter Property="Tag" Value="Search by name or type..."/>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- Health Filter -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="16,0,0,0">
                    <TextBlock Text="Status:" 
                               Foreground="{DynamicResource App.TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding HealthFilterOptions}"
                              SelectedItem="{Binding SelectedHealthFilter}"
                              Width="120"
                              FontSize="13"
                              Padding="8,6"/>
                </StackPanel>

                <!-- Sort -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="16,0,0,0">
                    <TextBlock Text="Sort:" 
                               Foreground="{DynamicResource App.TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding SortOptions}"
                              SelectedItem="{Binding SelectedSort}"
                              Width="130"
                              FontSize="13"
                              Padding="8,6"/>
                </StackPanel>

                <!-- Show Closed Toggle -->
                <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="16,0,0,0">
                    <CheckBox IsChecked="{Binding ShowClosed}"
                              VerticalAlignment="Center"/>
                    <TextBlock Text="Show Closed" 
                               Foreground="{DynamicResource App.TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="6,0,0,0"/>
                </StackPanel>

                <!-- Tag Filter -->
                <StackPanel Grid.Column="4" Orientation="Horizontal" Margin="16,0,0,0">
                    <TextBlock Text="Tag:" 
                               Foreground="{DynamicResource App.TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0"/>
                    <ComboBox ItemsSource="{Binding TagSuggestions}"
                              SelectedItem="{Binding SelectedTagFilter}"
                              Width="130"
                              FontSize="12"
                              Padding="6,4"
                              Background="{DynamicResource App.InputBackgroundBrush}"
                              Foreground="{DynamicResource App.InputForegroundBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content Area -->
        <Border Grid.Row="2" Background="{DynamicResource App.SurfaceBrush}" CornerRadius="12" Padding="20">
            <Grid>
                <!-- Empty State -->
                    <controls:EmptyStateControl 
                        IconText="&#xE8A5;"
                        Title="No obligations to display"
                    Message="Start by creating your first obligation. You'll be able to track loans, mortgages, subscriptions, and more."
                    ActionText="Create Obligation"
                    ActionCommand="{Binding CreateObligationCommand}"
                    IsActionVisible="True"
                    Visibility="{Binding HasNoObligations, Converter={StaticResource BoolToVis}}"/>

                <!-- Data Grid -->
                <DataGrid ItemsSource="{Binding ItemsView}"
                          SelectedItem="{Binding SelectedItem}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          Background="Transparent"
                          BorderThickness="0"
                              RowBackground="{DynamicResource App.GridRowBrush}"
                              AlternatingRowBackground="{DynamicResource App.GridAltRowBrush}"
                              Foreground="{DynamicResource App.TextPrimaryBrush}"
                          SelectionMode="Single"
                          Visibility="{Binding HasObligations, Converter={StaticResource BoolToVis}}">
                    <DataGrid.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick" 
                                      Command="{Binding OpenSelectedCommand}"
                                      CommandParameter="{Binding SelectedItem}"/>
                    </DataGrid.InputBindings>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" MinWidth="150"/>
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="100"/>
                        <DataGridTextColumn Header="Principal" Binding="{Binding Principal, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="Paid" Binding="{Binding Paid, StringFormat=N0}" Width="90"/>
                        <DataGridTextColumn Header="Outstanding" Binding="{Binding Outstanding, StringFormat=N0}" Width="100"/>
                        <DataGridTextColumn Header="Overdue" Binding="{Binding OverdueCount}" Width="70"/>
                        <DataGridTextColumn Header="Next Due" Binding="{Binding NextDueDateDisplay}" Width="100"/>
                        <DataGridTextColumn Header="Health" Binding="{Binding HealthStatus}" Width="80"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="70"/>
                        <DataGridTemplateColumn Header="Actions" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="Pay" 
                                                Command="{Binding DataContext.RecordPaymentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource SuccessButton}"
                                                ToolTip="Record Payment"/>
                                        <Button Content="Sched" 
                                                Command="{Binding DataContext.DefineScheduleCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                Style="{StaticResource ActionButton}"
                                                ToolTip="Define Schedule"/>
                                        <Button Content="Close" 
                                                Command="{Binding DataContext.CloseObligationCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}"
                                                ToolTip="Close Obligation">
                                            <Button.Style>
                                                <Style TargetType="Button" BasedOn="{StaticResource DangerButton}">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsClosed}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.RowSpan="3" 
                Background="{DynamicResource App.OverlayBrush}" 
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                CornerRadius="12">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                <TextBlock Text="Loading..." 
                           Foreground="{DynamicResource App.TextPrimaryBrush}"
                           FontSize="14"
                           Margin="0,12,0,0"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
